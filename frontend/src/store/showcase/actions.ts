import { ActionTree } from "vuex";
import { StateInterface } from "../index";
import { Showcase } from "./state";

const actions: ActionTree<Showcase, StateInterface> = {
  someAction(/* context */) {
    // your code
  },
};

export default actions;
